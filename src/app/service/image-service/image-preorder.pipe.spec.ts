import { ImagePreorderPipe } from './image-preorder.pipe';

describe('ImagePreorderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePreorderPipe();
    expect(pipe).toBeTruthy();
  });
});
